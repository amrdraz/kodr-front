{{component (concat 'challenge/' model.challenge.type '/challenge-trial-' model.challenge.type)
    model=model
    challenge=challenge
    run="run"
    rest="rest"
    test="test"
    viewDiscussion="viewDiscussion"
    }}

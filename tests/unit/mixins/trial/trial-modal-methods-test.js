import Ember from 'ember';
import TrialTrialModalMethodsMixin from '../../../mixins/trial/trial-modal-methods';
import { module, test } from 'qunit';

module('Unit | Mixin | trial/trial modal methods');

// Replace this with your real tests.
test('it works', function(assert) {
  var TrialTrialModalMethodsObject = Ember.Object.extend(TrialTrialModalMethodsMixin);
  var subject = TrialTrialModalMethodsObject.create();
  assert.ok(subject);
});

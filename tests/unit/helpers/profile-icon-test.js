import { profileIcon } from '../../../helpers/profile-icon';
import { module, test } from 'qunit';

module('Unit | Helper | profile icon');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = profileIcon(42);
  assert.ok(result);
});
